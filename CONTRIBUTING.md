# 기여 및 검증 기준

## 이슈와 PR

작업은 GitHub 이슈의 범위와 완료 조건을 기준으로 나눕니다. 최신 `main`에서 작업 브랜치를
만들고, 독립적으로 검토할 수 있는 PR 하나에 해당 구현·문서·검증을 함께 담습니다.
선행 이슈에 의존하는 작업은 해당 변경이 병합된 뒤 진행합니다.

커밋 메시지는 Conventional Commits 형식을 사용합니다. 기본 PR 상태는 Draft이며,
구현 이슈는 `Closes #번호`로 연결합니다. PR 생성과 이슈 완료를 구분하고 자동 병합하지 않습니다.
이슈·PR 설명은 한국어로 작성하고 코드와 식별자는 기존 표기를 유지합니다.

## 로컬 검증

```sh
pnpm install --frozen-lockfile
pnpm check
```

의존성은 사용하는 작업에서 추가하고 lockfile을 함께 갱신합니다. React·ReactDOM·StyleX의
외부 의존성과 ESM·타입·명시적 CSS import 계약을 유지합니다. 빌드 fixture와 테스트를 공개
API에 추가하거나 `dist`, tarball, 로그, 로컬 검토 자료를 커밋하지 않습니다.

현재 검증은 기반 설정과 배포물에 한정됩니다. Storybook·axe·시각 회귀 환경은 #3,
실제 Vite·Next.js production 소비 검증은 #7에서 추가합니다. 이 검사들을 실행하기 전에는
해당 호환성이나 접근성을 통과했다고 기록하지 않습니다.

## 독립 리뷰

커밋 전에 `gpt-6-astra` 모델과 `xhigh` 추론 수준을 **실행 옵션으로 지정한 새 컨텍스트**에서
독립 리뷰를 진행합니다. 리뷰어에게 요구사항, 신규 파일까지 포함한 고정 변경, 실제 검증
결과를 전달합니다. 요청 설정과 관찰된 실행 설정은 구분하고, 지정한 실행이 불가능하면
조용히 다른 모델로 대체하지 않습니다.

독립 리뷰는 동작·API·유지보수성·실패 경로와 불필요한 복잡성을 검토합니다. 우리 플러그인의
`requesting-code-review`와 `review-quality`가 있으면 사용하며, 설치 여부와 무관하게 위 기준을
지킵니다. 필수 수정 후 관련 검증과 재리뷰를 수행합니다. 여러 커밋의 PR은 전체 변경을 검토하고,
계획에 따른 작업 완료 시 일반 리뷰 뒤 별도 새 컨텍스트에서 목표·구현·검증의 연결을 반증하는
red-team 검토를 수행합니다.

## UI 변경에 적용할 기준

UI 검증 환경이 준비되면 상태별 Story·문서와 실제 조작 검증을 해당 컴포넌트 PR에 포함합니다.
기본/커스텀 테마, 긴 문구, 좁은 화면, focus·disabled·loading·error 등 지원하는 상태를 확인합니다.

axe는 Storybook 미리보기 문서와 소비 예제에서 실행하고, 열린 Dialog·Menu와 Portal도 포함합니다.
적용 규칙의 미해결 위반은 0건을 기준으로 하고 자동 판정 불가 항목은 수동으로 확인합니다.
키보드 순서, 포커스 가시성·복귀, Escape, 확대와 reduced motion도 별도로 검증합니다.
Storybook이라는 이유로 접근성 검사를 통째로 제외하지 않습니다.

`ux-audit`을 활용할 때는 실제 조작·시각 검토·증거 기록을 범용 UI에 맞춰 적용합니다.
서비스 persona나 실사용자 UX 검증을 수행한 것으로 표현하지 않습니다.
시각 baseline은 실제 화면과 디자인 기준을 비교한 뒤 승인하고 차이를 무조건 덮어쓰지 않습니다.

## 결과 기록

PR에는 변경 이유와 결과, 실행한 명령·결과, 미실행 항목·제약을 구분해 적습니다.
UI 변경에는 관련 Story와 검토 가능한 화면 증거를 포함합니다. 결함은 재현 절차·관찰/기대·영향·
관련 위치·증거·최소 수정안으로 기록하며 취향 제안과 필수 수정 사항을 구분합니다.
